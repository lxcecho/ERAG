# Lombok 配置：允许将 @Lazy 注解从字段复制到 @RequiredArgsConstructor 生成的构造器参数
# 用于打破 Spring Bean 循环依赖（如 KbDocumentService <-> KbParseTaskService）
config.stopBubbling = true
lombok.copyableAnnotations += org.springframework.context.annotation.Lazy
